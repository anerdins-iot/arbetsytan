# Dependencies and build output (must not be in context)
node_modules
.next
.env*
.git
.gitignore

# Prisma generated client (regenerated during build)
generated

# Dev/tooling
.dev-server.pid
playwright-report
test-results
.playwright-browsers
playwright-browsers
coverage
e2e
*.log

# Playwright (HUGE - 600+ MB)
.playwright
playwright.config.ts

# Docs and screenshots (not needed for build)
*.md
screenshots

# SQLite dev DB (production uses PostgreSQL)
*.db
prisma/*.db
