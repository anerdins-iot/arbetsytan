/** When conversation has more than this many messages, we return summary + only recent slice. */
export const MESSAGE_SUMMARY_THRESHOLD = 20;
/** Number of most recent messages to return when conversation has a summary. */
export const RECENT_MESSAGES_AFTER_SUMMARY = 15;
